import React, { useState, useEffect } from "react";
import { StyleSheet, View } from "react-native";

const NewScreen = ({ flujoPercentaje }) => {
    return (
        Hola
    );
};

const styles = StyleSheet.create({
    
});

export default NewScreen;
